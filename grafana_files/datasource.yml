apiVersion: 1

datasources:
- name: prometheus_hidra
  type: prometheus
  url: http://prometheus:9090 
  isDefault: true
  access: proxy
  editable: true
  basicAuth: true
  basicAuthUser: admin
  secureJsonData:
    basicAuthPassword: $HIDRA_AUTH_BASIC_PASSWORD